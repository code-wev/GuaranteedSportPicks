/**
 * Type definition for subscription.
 *
 * This type defines the structure of a single subscription object.
 * @interface TSubscription
 */
export interface TSubscription {
  // Add fields as needed
}